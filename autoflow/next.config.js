/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['graph.facebook.com', 'platform-lookaside.fbsbx.com', 'scontent.cdninstagram.com'],
  },
}

module.exports = nextConfig
